{
    "buildCommand": "next build",
    "version": "2",
    "build": [
        {
            "src": "/backend/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/.*",
            "dest": "/backend/app.js"
        }
    ]
}